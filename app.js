const KR = {
  home: {
    eyebrow: "WHOLESALE · TRADE · EXPORT",
    title: "산업을 잇는<br><em>공급의 흐름.</em>",
    intro:
      "다양한 산업 제품을 국내외로 공급하는 B2B 도매·무역·수출 전문 기업입니다.",
    about:
      "인트론글로벌 (INTRON GLOBAL)은 국내 도매·유통·수입판매 및 해외수출·중계무역 기업입니다.",
    services: [
      [
        "01",
        "국내 도매 유통",
        "국내 거래처 대상의 B2B 도매 유통 서비스 제공",
        "📦",
        "/domestic/",
      ],
      [
        "02",
        "무역 · 수출입",
        "국내 수입유통 및 해외 수출·중계무역",
        "🌐",
        "/export/",
      ],
      [
        "03",
        "무역·소싱 파트너",
        "해외 소싱 및 신규 품목 개발 등 무역 전반을 지원",
        "🤝",
        "/sourcing/",
      ],
    ],
    products: [
      [
        "01",
        "유리섬유/탄소섬유",
        "Chopped Strands · PA 6/66, PP, POM, PC, PET, PBT, ABS, SAN · 글라스 비드",
        "",
      ],
      [
        "02",
        "수지",
        "EP(PA, PC, PET, PBT), PP · POM, EVA, POE · 재생수지(PC, ASA, ABS)",
        "resin",
      ],
      [
        "03",
        "첨가제",
        "난연제 · 안티드립제 · 충격보강제 · 활제 · 산화방지제 · UV 안정제",
        "additive",
      ],
    ],
  },
  sections: {
    domestic: {
      title: "국내 판매<br>유통",
      kicker: "DOMESTIC DISTRIBUTION",
      lead: "INTRON GLOBAL은 안정적인 공급망과 유연한 납품 대응을 기반으로 국내 B2B 고객사에 맞춘 판매·유통 서비스를 제공합니다.",
      heading: "B2B 판매 유통",
      body: "제품의 안정적인 공급부터 재고 운영, 납기 대응까지. 거래처의 운영 환경에 맞춘 유통 프로세스를 설계합니다.",
      items: [
        "장기 공급 계약 대응",
        "소량 다품목 SKU 배송 지원",
        "국내 물류 및 유통 프로세스 연계",
        "재고 운영과 납기 대응 지원",
      ],
      visual: "LOCAL|NETWORK",
    },
    export: {
      title: "무역 ·<br>수출입",
      kicker: "TRADE · EXPORT · IMPORT",
      lead: "국내 유통부터 해외 수출, 중계무역까지 연결되는 실무형 무역 서비스를 제공합니다.",
      heading: "국경을 넘어<br>공급을 연결합니다.",
      body: "유럽, 영국, 터키, 중동 등 주요 시장을 대상으로 안정적인 수출과 국제 규정 대응을 지원합니다.",
      items: [
        "해외 수출: 주요 시장 대상 수출 및 국제 규정 대응",
        "중계무역: 대만 FORMOSA 및 중국 제조사 네트워크 기반 지원",
        "FTA 인증수출자: 유리섬유 제품 원산지 및 협정 적용 업무 지원",
      ],
      visual: "GLOBAL|ROUTE",
    },
    sourcing: {
      title: "무역·소싱<br>파트너",
      kicker: "SOURCING · CONSULTING",
      lead: "검증된 공급처 연결과 신규 품목 발굴을 통해 해외 파트너의 소싱과 개발 업무를 지원합니다.",
      heading: "소싱의 시작부터<br>거래의 완성까지.",
      body: "시장과 제품, 거래 조건을 함께 검토하며 실질적으로 작동하는 무역 파트너 역할을 수행합니다.",
      items: [
        "검증된 국내외 제조사 소싱",
        "시장 적용 가능한 신규 제품 발굴",
        "FOB, CIF, DAP 등 다양한 인코텀즈 대응",
        "무역 자문 및 실무 컨설팅 지원",
      ],
      visual: "SOURCE|TO SCALE",
    },
  },
  contact: {
    title: "문의하기",
    lead: "판매·무역·수출입 관련 문의 내용을 아래 양식으로 보내주시면 확인 후 회신드리겠습니다.",
  },
  footer: "거래의 다음 단계,<br>인트론글로벌과 함께하세요.",
};
const EN = {
  home: {
    eyebrow: "WHOLESALE · TRADE · EXPORT",
    title: "The flow of<br><em>industry supply.</em>",
    intro:
      "A B2B wholesale, trading and export company supplying industrial products across domestic and global markets.",
    about:
      "INTRON GLOBAL operates across domestic wholesale, distribution, import sales, overseas export and intermediary trade.",
    services: [
      [
        "01",
        "Domestic Distribution",
        "B2B wholesale and distribution for domestic partners",
        "📦",
        "/domestic-en/",
      ],
      [
        "02",
        "Trade · Export / Import",
        "Domestic import distribution, export and intermediary trade",
        "🌐",
        "/export-en/",
      ],
      [
        "03",
        "Sourcing Partner",
        "Global sourcing and new-product development support",
        "🤝",
        "/sourcing-en/",
      ],
    ],
    products: [
      [
        "01",
        "Fiberglass/Carbon Fiber",
        "Chopped Strands · PA 6/66, PP, POM, PC, PET, PBT, ABS, SAN · Glass Beads",
        "",
      ],
      [
        "02",
        "Resins",
        "EP(PA, PC, PET, PBT), PP · POM, EVA, POE · Recycled PC, ASA, ABS",
        "resin",
      ],
      [
        "03",
        "Additives",
        "Flame retardants · Anti-drip agents · Impact modifiers · Lubricants · Antioxidants · UV stabilizers",
        "additive",
      ],
    ],
  },
  sections: {
    domestic: {
      title: "Domestic<br>Distribution",
      kicker: "DOMESTIC DISTRIBUTION",
      lead: "INTRON GLOBAL provides B2B sales and distribution tailored to domestic customers through a stable supply chain and flexible delivery response.",
      heading: "B2B Distribution",
      body: "From reliable supply to inventory and delivery management, we build a distribution process around your operation.",
      items: [
        "Long-term supply contract support",
        "Small-quantity / multi-SKU delivery support",
        "Domestic logistics and distribution process linkage",
        "Inventory operation and delivery response support",
      ],
      visual: "LOCAL|NETWORK",
    },
    export: {
      title: "Trade ·<br>Export / Import",
      kicker: "TRADE · EXPORT · IMPORT",
      lead: "We provide practical trade services spanning domestic distribution, overseas export and intermediary trade.",
      heading: "Connecting supply<br>beyond borders.",
      body: "We support reliable exports and international compliance for major markets including Europe, the UK, Türkiye and the Middle East.",
      items: [
        "Overseas export and international compliance support",
        "Intermediary trade using FORMOSA and Chinese manufacturer networks",
        "FTA approved exporter support for fiberglass products",
      ],
      visual: "GLOBAL|ROUTE",
    },
    sourcing: {
      title: "Trade &<br>Sourcing Partner",
      kicker: "SOURCING · CONSULTING",
      lead: "We support overseas partners with sourcing and development through verified suppliers and new-product discovery.",
      heading: "From sourcing<br>to a completed deal.",
      body: "We review markets, products and trade conditions together to act as a practical trade partner.",
      items: [
        "Verified domestic and overseas manufacturer sourcing",
        "New-product discovery for market application",
        "Support for FOB, CIF, DAP and other Incoterms",
        "Trade advisory and practical consulting",
      ],
      visual: "SOURCE|TO SCALE",
    },
  },
  contact: {
    title: "Contact",
    lead: "Send us your sales, trade or import/export inquiry through the form below and we will respond after review.",
  },
  footer: "Take the next step<br>in trade with us.",
};
const page = document.body.dataset.page,
  ko = document.body.dataset.lang !== "en",
  d = ko ? KR : EN,
  homeUrl = ko ? "/" : "/index-en/",
  contactUrl = ko ? "/contact/" : "/contact-en/",
  address = ko
    ? "경기도 용인시 기흥구 강남로 9, 503-439호 (구갈동, 태평양프라자)"
    : "503-439, 9 Gangnam-ro, Giheung-gu, Yongin-si, Gyeonggi-do, Korea";
const t = (a, b) => (ko ? a : b);
const nav = ko
  ? [
      ["회사소개", "/#about"],
      ["사업분야", "/#services"],
      ["문의하기", "/contact/"],
    ]
  : [
      ["About", "/index-en/#about"],
      ["Services", "/index-en/#services"],
      ["Contact", "/contact-en/"],
    ];
function languageLink() {
  let p = location.pathname;
  if (ko) {
    if (p === "/") return "/index-en/";
    return p.replace(/\/$/, "-en/");
  }
  return p.replace(/-en\//, "/");
}
function header() {
  return `<div class="topline"></div><header class="header wrap"><a class="brand" href="${homeUrl}"><span class="brand-mark">IG</span>INTRON GLOBAL</a><nav class="nav">${nav.map((x) => `<a href="${x[1]}">${x[0]}</a>`).join("")}</nav><div class="lang"><a class="${ko ? "active" : ""}" href="${ko ? location.pathname : location.pathname.replace("-en/", "/")}">KR</a> / <a class="${ko ? "" : "active"}" href="${languageLink()}">EN</a></div><button class="menu" aria-label="menu">☰</button></header>`;
}
function footer() {
  return `<footer class="footer"><div class="wrap"><div class="footer-top"><div><div class="eyebrow">INTRON GLOBAL</div><h2>${d.footer}</h2></div><a class="button primary" href="${contactUrl}">${t("거래 문의하기", "Contact us")} <b>↗</b></a></div><div class="footer-bottom"><div><strong>${t("(주)인트론글로벌", "INTRON GLOBAL CO., LTD.")}</strong><br>${t("사업분야: 도매업 · 무역업(수출·수입)", "Wholesale · Trade · Export / Import")}<br>${address}</div><div>intron1101@gmail.com<br>FAX. 0504-423-5748<br>© 2025 INTRON GLOBAL CO., LTD.</div></div></div></footer>`;
}

function supplyChain() {
  const factoryIcon = `<svg viewBox="0 0 120 96" aria-hidden="true"><path d="M10 84V42l26 16V42l28 16V20h18v64M10 84h100M22 84V68h14v16m14 0V68h14v16m14 0V68h14v16M64 20V8h18v12"/><path d="M10 42 36 26v16l28-16"/></svg>`;
  const marketIcon = `<svg viewBox="0 0 132 96" aria-hidden="true"><path d="M12 84V44h68v40M8 44l8-20h60l8 20M22 24v20m18-20v20m18-20v20M26 84V66h18v18M94 84V48m0 12c0-12 10-22 22-22s22 10 22 22v24M102 52c3 5 8 8 14 8s11-3 14-8M104 84V72h24v12"/><circle cx="116" cy="25" r="13"/><path d="M116 12v26m-13-13h26"/></svg>`;

  return `<div class="supply-chain" aria-label="${t("제조사부터 시장과 고객까지 연결하는 공급망", "Supply chain from manufacturers to markets and customers")}">
    <i class="atomic-orbit orbit-one" aria-hidden="true"></i>
    <i class="atomic-orbit orbit-two" aria-hidden="true"></i>
    <i class="atomic-orbit orbit-three" aria-hidden="true"></i>
    <div class="orbit-carrier orbit-source-carrier">
      <article class="chain-node chain-source">
        <div class="planet-core"><div class="chain-graphic">${factoryIcon}</div></div>
        <div class="planet-caption"><h4>${t("제조사", "Manufacturers")}</h4><p>${t("제품 생산 · 안정적 조달", "Production · Secure sourcing")}</p></div>
      </article>
    </div>
    <div class="supply-link" aria-hidden="true"><span>SUPPLY</span></div>
    <article class="chain-node chain-intron">
      <div class="planet-core"><div class="chain-logo"></div></div>
      <div class="planet-caption"><h4>INTRON GLOBAL</h4><p>${t("검수 · 유통 · 무역", "Quality · Distribution · Trade")}</p></div>
    </article>
    <div class="supply-link" aria-hidden="true"><span>DELIVER</span></div>
    <div class="orbit-carrier orbit-market-carrier">
      <article class="chain-node chain-market">
        <div class="planet-core"><div class="chain-graphic">${marketIcon}</div></div>
        <div class="planet-caption"><h4>${t("시장 및 고객", "Markets & clients")}</h4><p>${t("국내 · 해외 공급", "Domestic · Global delivery")}</p></div>
      </article>
    </div>
  </div>`;
}

function home() {
  const h = d.home;
  return `${header()}<main><section class="hero"><div class="wrap hero-grid"><div><div class="eyebrow">${h.eyebrow}</div><h1>${h.title}</h1><p>${h.intro}</p><div class="hero-actions"><a class="button primary" href="${contactUrl}">${t("거래 문의하기", "Start a conversation")} <b>↗</b></a><a class="button" href="#services">${t("사업분야 보기", "Explore services")} <b>↓</b></a></div></div><aside class="signal"><div class="signal-title">Supply signal / 01</div><b>${t("국내 · 해외를 잇다", "Connected supply")}</b><div class="signal-row"><span>${t("주요 지역", "Coverage")}</span><strong>${t("국내 · 해외", "Domestic · Global")}</strong></div><div class="signal-row"><span>${t("거래 형태", "Business")}</span><strong>B2B</strong></div></aside></div></section><div class="ticker"><div>INTRON GLOBAL — WHOLESALE — TRADE — EXPORT — DISTRIBUTION — SOURCING — &nbsp; INTRON GLOBAL — WHOLESALE — TRADE — EXPORT — DISTRIBUTION — SOURCING —</div></div><section id="about" class="section light"><div class="wrap about-grid">${supplyChain()}<div class="about-copy"><div><div class="section-label">01 / ABOUT</div><h3>${t("신뢰를 기반으로,<br>거래의 다음을 설계합니다.", "Built on trust.<br>Designed for what’s next.")}</h3></div><div><div class="tags"><span>${t("도매업", "WHOLESALE")}</span><span>${t("무역업", "TRADE")}</span><span>${t("수출입", "EXPORT / IMPORT")}</span></div><p>${h.about}</p></div><div class="contact-line">${address}<br>intron1101@gmail.com</div></div></div></section><section id="services" class="section"><div class="wrap"><div class="section-head"><div><div class="section-label">02 / SERVICES</div><h2>${t("필요한 방식으로,<br>정확하게 연결합니다.", "Services that connect<br>with precision.")}</h2></div><p class="section-intro">${t("도매 유통부터 해외 수출과 소싱까지, 상황에 맞는 공급 방식을 제안합니다.", "From distribution to export and sourcing, we propose the right supply approach for your business.")}</p></div><div class="service-grid">${h.services.map((x) => `<a class="service-card" href="${x[4]}"><div class="service-num">${x[0]}</div><div class="service-icon">${x[3]}</div><h3>${x[1]}</h3><p>${x[2]}</p><span class="arrow">↗</span></a>`).join("")}</div></div></section><section class="section" style="background:#0a2032"><div class="wrap product-layout"><div><div class="section-label">03 / PRODUCTS</div><h2>${t("산업의 기반이 되는<br>소재를 공급합니다.", "Materials that power<br>industry.")}</h2><p class="product-note">${t("거래 목적과 적용 환경에 맞는 제품을 안정적으로 제안합니다.", "We provide products reliably for the purpose and application environment of every deal.")}</p></div><div class="products">${h.products.map((x) => `<article class="product"><div class="product-no">${x[0]}</div><div><h3>${x[1]}</h3><p>${x[2]}</p></div><div class="material ${x[3]}"></div></article>`).join("")}</div></div></section></main>${footer()}`;
}
function detail(k) {
  let s = d.sections[k];
  return `${header()}<main><section class="sub-hero"><div class="wrap"><a class="back" href="${homeUrl}">← ${t("메인으로", "Back to home")}</a><div class="eyebrow">${s.kicker}</div><h1>${s.title}</h1><p>${s.lead}</p></div></section><section class="section light"><div class="wrap detail-grid"><div class="detail-copy"><div class="section-label">INTRON GLOBAL / ${s.kicker}</div><h2>${s.heading}</h2><p>${s.body}</p><div class="list">${s.items.map((x, i) => `<div><b>0${i + 1}</b><span>${x}</span></div>`).join("")}</div></div><div class="detail-visual"><b>${s.visual.replace("|", "<br>")}</b></div></div>${k === "export" ? `<div class="trade-cards"><div class="trade-card"><strong>EXPORT</strong><p>${t("주요 시장을 대상으로 안정적인 수출과 국제 규정 대응을 지원합니다.", "Reliable export and international compliance support for major markets.")}</p></div><div class="trade-card"><strong>INTERMEDIARY</strong><p>${t("대만 FORMOSA 및 중국 제조사 네트워크 기반의 중계무역을 지원합니다.", "Intermediary trade based on FORMOSA and Chinese manufacturer networks.")}</p></div><div class="trade-card"><strong>FTA</strong><p>${t("유리섬유 제품 FTA 인증수출자 업무를 지원합니다.", "FTA approved exporter support for fiberglass products.")}</p></div></div>` : ""}</div></section></main>${footer()}`;
}
function contact() {
  return `${header()}<main><section class="sub-hero"><div class="wrap"><a class="back" href="${homeUrl}">← ${t("메인으로", "Back to home")}</a><div class="eyebrow">CONTACT / INTRON GLOBAL</div><h1>${d.contact.title}</h1><p>${d.contact.lead}</p></div></section><section class="section light contact-section"><div class="wrap contact-grid"><div class="contact-info"><h2>${t("함께할 거래를<br>기다립니다.", "Let’s discuss<br>what’s next.")}</h2><p>${t("입력하신 이메일로 회신드리므로 정확한 주소를 입력해 주세요.", "Please enter an accurate email address so we can respond to your inquiry.")}</p><div class="info-block"><div><span>EMAIL</span>intron1101@gmail.com</div><div><span>FAX</span>0504-423-5748</div><div><span>ADDRESS</span>${address}</div></div></div><form class="form"><div class="field"><label>${t("회사명", "COMPANY")}</label><input required placeholder="${t("회사명을 입력해 주세요", "Your company")}"></div><div class="field"><label>${t("담당자명", "NAME")}</label><input required placeholder="${t("성함을 입력해 주세요", "Your name")}"></div><div class="field full"><label>EMAIL</label><input required type="email" placeholder="email@company.com"></div><div class="field full"><label>${t("문의 내용", "MESSAGE")}</label><textarea required placeholder="${t("문의하실 내용을 입력해 주세요", "Tell us about your inquiry")}"></textarea></div><p class="notice">${t("문의 접수 후 확인하여 회신드립니다.", "We will review your inquiry and respond by email.")}</p><button class="button primary" type="submit">${t("문의 보내기", "Send inquiry")} <b>↗</b></button></form></div></section></main>${footer()}`;
}
document.getElementById("app").innerHTML =
  page === "home" ? home() : page === "contact" ? contact() : detail(page);

if (page === "home") {
  const signalTitle = document.querySelector(".signal-title");
  if (signalTitle) signalTitle.textContent = "Supply signal";

  document.querySelectorAll(".section-label").forEach((label) => {
    label.textContent = label.textContent.replace(/^\d+\s*\/\s*/, "");
  });
  document.querySelectorAll(".service-num, .product-no").forEach((item) => item.remove());
}

// 주소만 노출하고 이메일과 팩스는 숨긴다.
document.querySelectorAll(".contact-line").forEach((item) => {
  item.textContent = address;
});
document.querySelectorAll(".info-block").forEach((item) => {
  item.innerHTML = `<div><span>ADDRESS</span>${address}</div>`;
});

const footerLegal = document.querySelector(".footer-bottom > div:last-child");
if (footerLegal) {
  while (footerLegal.childNodes.length > 1) footerLegal.firstChild.remove();
}

document
  .querySelector(".menu")
  ?.addEventListener("click", () =>
    document.querySelector(".nav").classList.toggle("open"),
  );
function loadEmailJs() {
  if (window.emailjs) return Promise.resolve(window.emailjs);

  return new Promise((resolve, reject) => {
    const script = document.createElement("script");
    script.src = "https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js";
    script.onload = () => resolve(window.emailjs);
    script.onerror = () => reject(new Error("EmailJS load failed"));
    document.head.append(script);
  });
}

const CONTACT_HISTORY_KEY = "intron_contact_history";
const CONTACT_COOLDOWN_MS = 60 * 1000;
const DUPLICATE_BLOCK_MS = 24 * 60 * 60 * 1000;
const MAX_ATTACHMENT_SIZE = 20 * 1024 * 1024;
const ALLOWED_ATTACHMENT_TYPES = [
  "application/pdf",
  "application/msword",
  "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  "application/vnd.ms-excel",
  "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
  "image/jpeg",
  "image/png",
  "text/plain",
];
const SPAM_PATTERNS = [
  /(?:검색\s*엔진\s*최적화|\bSEO\b)/i,
  /(?:백링크|게스트\s*포스트|트래픽\s*보장)/i,
  /(?:대출|카지노|성인\s*사이트)/i,
];

function createContentHash(value) {
  let hash = 2166136261;
  for (let i = 0; i < value.length; i += 1) {
    hash ^= value.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36);
}

function getContactHistory() {
  try {
    return JSON.parse(localStorage.getItem(CONTACT_HISTORY_KEY)) || {};
  } catch (_) {
    return {};
  }
}

function saveContactHistory(contentHash) {
  try {
    localStorage.setItem(CONTACT_HISTORY_KEY, JSON.stringify({ sentAt: Date.now(), contentHash }));
  } catch (_) {
    // 저장소를 사용할 수 없어도 문의 전송은 허용한다.
  }
}

function validateInquiry(subject, message) {
  const combined = `${subject} ${message}`.trim();
  const contentHash = createContentHash(combined.toLowerCase().replace(/\s+/g, " "));
  const history = getContactHistory();
  const elapsed = Date.now() - Number(history.sentAt || 0);

  if (message.length < 20) {
    return { error: t("문의 내용은 20자 이상 입력해 주세요.", "Please enter at least 20 characters in your message.") };
  }
  if ((combined.match(/(?:https?:\/\/|www\.)/gi) || []).length > 1) {
    return { error: t("링크는 1개까지만 입력할 수 있습니다.", "Only one link is allowed.") };
  }
  if (SPAM_PATTERNS.some((pattern) => pattern.test(combined))) {
    return { error: t("광고 또는 스팸성 문의는 전송할 수 없습니다.", "Promotional or spam inquiries cannot be sent.") };
  }
  if (elapsed < CONTACT_COOLDOWN_MS) {
    return { error: t("연속 전송은 60초 후에 다시 시도해 주세요.", "Please wait 60 seconds before sending another inquiry.") };
  }
  if (history.contentHash === contentHash && elapsed < DUPLICATE_BLOCK_MS) {
    return { error: t("동일한 문의는 24시간 내에 다시 전송할 수 없습니다.", "The same inquiry cannot be resent within 24 hours.") };
  }
  return { contentHash };
}

async function uploadAttachment(file) {
  const formData = new FormData();
  formData.append("file", file);
  const response = await fetch("https://file.io", { method: "POST", body: formData });
  const data = await response.json();

  if (data?.success && data.link) return data.link;
  if (data?.link) return data.link;
  throw new Error("Attachment upload failed");
}

const emailForm = document.querySelector(".form");
if (emailForm) {
  const attachmentField = document.createElement("div");
  attachmentField.className = "field full attachment-field";
  attachmentField.innerHTML = `<label>${t("첨부파일", "ATTACHMENT")}</label><input id="attachment" type="file" accept=".pdf,.doc,.docx,.xls,.xlsx,.txt,.jpg,.jpeg,.png"><small class="attachment-hint">${t("선택 사항 · 최대 20MB · PDF, Office, TXT, JPG, PNG", "Optional · Up to 20MB · PDF, Office, TXT, JPG, PNG")}</small><span class="attachment-status" aria-live="polite"></span>`;
  emailForm.insertBefore(attachmentField, emailForm.querySelector(".notice"));

  const attachmentInput = attachmentField.querySelector("input");
  const attachmentStatus = attachmentField.querySelector(".attachment-status");

  attachmentInput.addEventListener("change", () => {
    const file = attachmentInput.files[0];
    if (!file) {
      attachmentStatus.textContent = "";
      return;
    }
    if (file.size > MAX_ATTACHMENT_SIZE) {
      attachmentStatus.textContent = t("파일은 20MB 이하만 첨부할 수 있습니다.", "Files must be 20MB or smaller.");
      attachmentInput.value = "";
      return;
    }
    if (!ALLOWED_ATTACHMENT_TYPES.includes(file.type)) {
      attachmentStatus.textContent = t("지원하지 않는 파일 형식입니다.", "This file type is not supported.");
      attachmentInput.value = "";
      return;
    }
    attachmentStatus.textContent = `${t("선택된 파일:", "Selected file:")} ${file.name} (${Math.round(file.size / 1024)} KB)`;
  });

  const honeypot = document.createElement("input");
  honeypot.type = "text";
  honeypot.name = "hp_field";
  honeypot.autocomplete = "off";
  honeypot.tabIndex = -1;
  honeypot.setAttribute("aria-hidden", "true");
  honeypot.style.cssText = "position:absolute;left:-9999px;width:1px;height:1px;overflow:hidden;";
  emailForm.append(honeypot);

  emailForm.addEventListener(
    "submit",
    async (e) => {
      e.preventDefault();
      e.stopImmediatePropagation();

      const [companyInput, nameInput, emailInput] = emailForm.querySelectorAll("input");
      const messageInput = emailForm.querySelector("textarea");
      const submitButton = emailForm.querySelector('button[type="submit"]');
      const originalLabel = submitButton.innerHTML;
      const subject = companyInput.value.trim()
        ? `${t("홈페이지 문의", "Website inquiry")} | ${companyInput.value.trim()}`
        : t("홈페이지 문의", "Website inquiry");

      if (honeypot.value.trim() !== "") {
        alert(t("전송 중 오류가 발생했습니다.", "An error occurred while sending."));
        return;
      }

      const validation = validateInquiry(subject, messageInput.value.trim());
      if (validation.error) {
        alert(validation.error);
        return;
      }

      submitButton.disabled = true;
      submitButton.textContent = t("전송 중...", "Sending...");

      try {
        let attachmentUrl = "";
        const file = attachmentInput.files[0];
        if (file) {
          if (file.size > MAX_ATTACHMENT_SIZE) throw new Error("Attachment exceeds 20MB");
          if (!ALLOWED_ATTACHMENT_TYPES.includes(file.type)) throw new Error("Unsupported attachment type");
          attachmentStatus.textContent = t("첨부파일 업로드 중...", "Uploading attachment...");
          attachmentUrl = await uploadAttachment(file);
          attachmentStatus.textContent = t("첨부파일 업로드 완료", "Attachment uploaded");
        }

        const emailjs = await loadEmailJs();
        // 기존 문의 페이지의 EmailJS 설정을 그대로 사용한다.
        emailjs.init("S_pZX4esOTmL2yED9");
        await emailjs.send("INTRON MAIL", "template_ahovfta", {
          name: nameInput.value.trim(),
          email: emailInput.value.trim(),
          subject,
          message: messageInput.value.trim(),
          attachment_url: attachmentUrl,
        });
        saveContactHistory(validation.contentHash);
        alert(t("문의가 정상적으로 전송되었습니다.", "Your inquiry has been sent."));
        emailForm.reset();
        attachmentStatus.textContent = "";
      } catch (error) {
        console.error(error);
        alert(t("전송에 실패했습니다. 잠시 후 다시 시도해 주세요.", "Sending failed. Please try again shortly."));
      } finally {
        submitButton.disabled = false;
        submitButton.innerHTML = originalLabel;
      }
    },
    true,
  );
}

document.querySelector(".form")?.addEventListener("submit", (e) => {
  e.preventDefault();
  let x = document.createElement("div");
  x.className = "toast";
  x.textContent = t(
    "문의 양식이 준비됐어. 이메일로 전달해 줘.",
    "Your inquiry is ready. Please send it by email.",
  );
  document.body.append(x);
  requestAnimationFrame(() => x.classList.add("show"));
  setTimeout(() => x.remove(), 3500);
});
